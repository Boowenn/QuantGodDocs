# Frontend 文档入口

QuantGodFrontend 是 Vue operator workbench。它通过 service layer 调用 `QuantGodBackend` 的 `/api/*`，不再直接读取 `QuantGod_*.json` 或 `QuantGod_*.csv`。

核心文档：

- [工作台说明](workbench.md)
- [API Client 规则](api-client.md)
